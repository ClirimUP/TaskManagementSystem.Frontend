# VITE_API_URL - Backend API base URL (no trailing slash)
VITE_API_URL=http://localhost:5196

# VITE_AUTH_ENABLED - Enable/disable authentication
# "true"  = show login/register, protect task routes, send auth headers
# "false" = no auth UI, no auth headers
VITE_AUTH_ENABLED=true
